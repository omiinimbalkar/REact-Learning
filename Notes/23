***** passing children

so simple as ki container to store a data inside  
h1 pass inside thee container 
h1 attribute

so simple ki actual data if we want pass in the foorm of passing childer we pass by using props ok inside compoents
so example in using the fooditem project 
